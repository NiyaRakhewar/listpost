import React from "react";
import "./../styles/Shimmer.css"; // Import the CSS for the shimmer effect

const Shimmer = () => {
  return (
      <div className="shimmer-card"></div>
    
  );
};

export default Shimmer;
